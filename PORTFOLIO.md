# 작업 요약 — 한빈 (2026-08-25 ~ 09-10)

> 입찰메이트(BidFit) RFP RAG 프로젝트 / 브랜치 `feat/rag-pipeline-and-eval`
> 최종 보고서 작성을 위한 개인 작업 정리본입니다. 수치는 전부 실측값이며, 측정 대상(골든셋·표본 수)을 함께 적었습니다.

## 1. 담당 범위

데이터 전처리부터 검색·생성 평가, 성능 개선 실험, 그리고 서빙 앱과 배포 구성까지 파이프라인 전 구간을 맡았습니다. 팀원이 만든 자산(검증 골든셋 111건, `golden-set-v3-share` 평가 패키지)은 가져다 쓰되, 그 자산의 문제점을 검증해 교정하는 작업도 포함했습니다.

일관되게 지킨 원칙이 두 가지입니다. **선호가 아니라 측정으로 결정한다** — 도입한 것보다 실측 후 기각한 것이 더 많습니다. 그리고 **자동으로 답을 확정하지 않고 후보를 제시한다** — 예산·마감일처럼 사람이 확인해야 하는 값은 후보와 근거를 보여주고 판단을 사람에게 남겼습니다.

## 2. 단계별 진행

| 기간 | 단계 | 핵심 내용 |
| --- | --- | --- |
| 08-25~08-28 | 데이터 전처리 | hwp/pdf 파싱 안정화, 표 처리 구조 문제 발견·해결 |
| 08-31~09-01 | 평가 기반 구축 | 골든셋 111건 전환·교정, Retrieval 평가, Generation baseline 최초 구축 |
| 09-02~09-03 | 성능 개선 실험 | Parent-Child, 임베딩 A/B, 리랭커, 가중치 튜닝, 프롬프트 v2 |
| 09-06~09-07 | 원인 규명 | chunk 단위 검색 구멍 발견, 잘못된 기권의 원인 규명 |
| 09-08~09-09 | 서빙 프로토타입 | 자주 묻는 질문 후보 추출기 11종, 스트림릿 화면 |
| 09-10 | 서빙 앱·배포 | FastAPI 앱 + 웹 UI, 도커 배포 구성, GCP VM 배포 문서 |

## 3. 최종 시스템 구성

| 구분 | 채택안 | 결정 근거 |
| --- | --- | --- |
| 임베딩 | KURE-v1 (로컬·무료) | text-embedding-3-small과 A/B 실측, 전 지표 우세 |
| 검색 | Hybrid (BM25 + 벡터, 0.5/0.5) | 가중치 0.0~1.0 스윕 결과 기본값이 최적 |
| 청킹 | Parent-Child (검색은 child, 컨텍스트는 parent) | fact coverage 개선 확인 |
| 리랭커 | 미도입 | cross-encoder 2종 모두 hybrid 단독보다 나빠 기각 |
| 생성 | gpt-5-mini (Responses API) | 서빙 화면에서 gpt-5-nano와 전환 비교 가능 |

## 4. 성능 수치

**Retrieval — 팀 공식 골든셋 111건**

| 지표 | 값 |
| --- | ---: |
| recall@1 | 0.865 |
| recall@3 | 0.946 |
| recall@5 | 0.964 |
| coverage@5 | 0.904 |
| MRR | 0.911 |

**Retrieval — 팀원 기준선과 동일 조건 비교 (rag-56, 54건)**

골든셋을 팀원이 쓴 것과 정확히 통일해 같은 문항으로 비교했습니다.

| 지표 | 우리 (KURE-v1 + Hybrid) | 팀원 API 기준선 | 격차 |
| --- | ---: | ---: | ---: |
| recall@1 | 0.870 | 0.753 | +0.117 |
| recall@5 | 0.981 | 0.846 | +0.135 |
| recall@10 | 1.000 | 0.846 | +0.154 |
| MRR@10 | 0.926 | 0.825 | +0.101 |
| nDCG@10 | 0.935 | 0.811 | +0.124 |

**Generation — rag-56 (결정론적 채점 3종, LLM 판정 없음)**

| 지표 | 값 | 대상 |
| --- | ---: | ---: |
| 기권 판단 일치율 | 0.946 | 56건 |
| 필요 문서 인용률 | 0.935 | 54건 |
| 어휘 기반 사실 커버리지(참고용) | 0.566 | 53건 |

**Parent-Child 효과 (context fact coverage, 111건)**

완전 커버율 36.1% → 40.7%, 평균 coverage 45.2% → 51.9%. 비용은 recall@5가 0.982 → 0.973으로 0.9%p 하락.

## 5. 실측 후 기각·유지한 것

보고서에서 "왜 이 구성인가"를 설명할 때 쓸 수 있는 부분입니다. 도입하지 않기로 한 결정도 전부 수치 근거가 있습니다.

- **리랭커 기각** — BAAI/bge-reranker-v2-m3, Dongjin-kr/ko-reranker 두 후보 모두 전 지표 소폭 악화(recall@5 −2.7%p). 원인은 두 가지로, 필터·집계형 질문이 cross-encoder 구조와 맞지 않고, RFP 표준 조항은 기관만 다르고 문구가 거의 같아 리랭커가 어느 기관 문서인지 구별하지 못해 BM25가 잡던 신호를 눌러버렸습니다.
- **하이브리드 가중치 유지** — 0.0~1.0을 0.1 간격으로 스윕한 결과 현재 기본값 0.5가 11개 후보 중 5개 지표 평균 1위. 특정 지표만 근소하게 앞서는 값이 있어도 다른 지표에서 더 크게 손해 보는 트레이드오프였습니다.
- **임베딩 유지** — KURE-v1이 OpenAI text-embedding-3-small 대비 전 지표 우세(recall@1 0.865 vs 0.676). 유료 API로 바꿀 이유가 없다는 결론.
- **표·그림 위치 채점 보류** — 코퍼스의 96%가 HWP인데 HWP는 파일 구조상 페이지 개념이 없어(pyhwp가 렌더링 없이 텍스트만 추출) 페이지·좌표 단위 채점을 하려면 렌더링 서브시스템을 새로 만들어야 합니다. 대상이 10건뿐이라 투자 대비 효과가 낮다고 판단해 보류했습니다.

## 6. 발견해서 고친 문제

품질 관리 측면에서 보고서에 넣을 만한 항목들입니다. 대부분 "수치가 이상해서 원문을 직접 열어보다가" 발견했습니다.

**전처리 단계**

- pyhwp가 표·이미지를 실제 내용 대신 `<표>`/`<그림>` 자리표시자로만 추출하던 문제 — 전체 hwp 문서에 영향. 제거 로직 추가 후 캐시 재적용.
- PDF 표 추출이 본문과 완전 중복 — 대상 PDF 4건의 표 834개를 전수 검사한 결과 완전중복 95%, 부분중복 5%, **고유 정보 0%**. 중복 필터를 넣지 않았다면 임베딩에 순수 노이즈를 쌓고 있었을 상황이었습니다.
- **표 추출 결과의 비결정성** — 재파싱할 때마다 "표가 있는 문서 수"가 30 → 62 → 94 → 97건으로 달라지는 현상. 원인은 hwp5html 서브프로세스 타임아웃(90초)이 문서의 36%에 부족했고 그 순간의 시스템 부하에 따라 성공/실패가 갈리던 것이었습니다. 타임아웃 상향 + 개별 재시도로 97/97건 전부 성공. 실험 재현성 자체를 위협하던 문제였습니다.

**평가 단계**

- 채점 코드 버그 5종 수정 — 금액 쉼표 표기, 조사·괄호가 낀 단어 순서, 날짜 표기 차이, 다중 답변을 완전 기권으로 오분류, 인용 파일명이 대괄호에서 잘리는 문제. 마지막 두 개는 재생성 없이 재채점만으로 각각 8건·6건의 오판을 정정했습니다. 수정 전 수치를 그대로 팀에 공유했다면 우리 성능이 실제보다 낮게 보고될 뻔했습니다.
- 골든셋 정답이 파이프라인에서 이미 제외한 중복 문서를 가리키던 3건 자동 교정 — 아무리 잘 찾아도 recall이 오를 수 없는 구조였습니다.
- 골든셋에 검색 대상이 아닌 출처(CSV 메타데이터)가 섞여 있던 문제 — recall이 부당하게 깎이지 않도록 필터링.

## 7. 가장 중요한 발견: 문서는 찾는데 조각을 못 찾는다 (09-06)

doc 단위 recall만 보면 0.97~1.0이라 "검색은 거의 완벽하다"고 판단해왔는데, chunk 단위로 다시 재보니 실제로는 구멍이 있었습니다.

| 지표 | 값 |
| --- | ---: |
| doc 단위 recall@5 | 0.972 |
| **chunk 단위 recall@5** | **0.611** |
| 문서는 맞는데 조각은 틀린 비율 | 36% (13/36건) |

원인을 13건 전부 들여다본 결과 두 패턴이었습니다. 하나는 **개요·표지 조각이 랭킹을 장악**하는 것으로, 사업명 고유명사가 문서 앞부분에 반복 등장해 질의와의 매칭 점수가 높게 나오고 정작 뒤쪽 세부 조항은 20위 밖으로 밀립니다. 다른 하나는 **표준 서식 교란**으로, 여러 RFP에 거의 동일한 문구로 반복되는 조항이 질문과 무관한 다른 문서에서 끌려옵니다.

이 발견으로 Parent-Child의 효과도 재해석해야 했습니다. chunk를 더 정확히 찾아서가 아니라, **정확한 조각을 못 찾아도 parent 확장이 주변 맥락을 통째로 끌어와 구멍을 메워준** 안전망 효과였을 가능성이 큽니다.

이어서 09-07에는 "답해야 하는데 기권한" 케이스의 원인을 검색 실패와 생성 실패로 자동 분류하는 도구를 만들어, 원인을 가릴 수 있었던 2건 모두 **검색 실패**임을 확인했습니다. 09-03에 사람이 눈으로 보고 "프롬프트가 모델을 소극적으로 만들었다"고 내렸던 판단이 틀렸다는 것도 이 과정에서 확인해 정정했습니다.

## 8. 서빙 (09-08 ~ 09-10)

**자주 묻는 질문 후보 추출기 11종** — 골든셋 180문항을 주제별로 훑어 빈도 순으로 항목을 선정했습니다(하도급·공동수급 15건, 사업목적 13건, 평가배점 9건, 계약방식 8건, 참가자격 8건, 보증금 6건 등). 전체 코퍼스 기준 항목별 후보 발견율은 하도급·사업목적 99~100%, 참가자격 98%, 문의처 90%, 평가배점 89%, 계약방식 88%, 신청서식 85~87%, 계약보증금 44%입니다.

계약보증금은 세 차례 개선으로 22.4% → 43.9%까지 올렸고(분수 표기, 줄바꿈이 낀 경우, 천분율·전각 % 인식), 남은 미검출 건은 원문을 직접 확인한 결과 **문서 자체에 비율이 적혀 있지 않은 표준 서식**이라 정규식의 한계가 아님을 확인했습니다.

**웹 앱** — FastAPI 백엔드 + 순수 HTML/JS 화면. 문서를 고르고, 버튼 11종으로 후보를 확인하고, 선택적으로 LLM 요약을 받고, 그 문서 범위의 자유 질문(기존 Hybrid 검색 재사용)을 할 수 있습니다. 대화 상태를 서버가 아닌 브라우저가 들고 있어 여러 명이 동시에 접속해도 화면이 섞이지 않습니다. 무거운 자산은 지연 로드라 버튼만 쓰면 2.2GB 임베딩 모델을 아예 올리지 않습니다.

**배포** — CPU 전용 도커 이미지. 서버리스(버셀 등)는 요청 사이에 메모리가 유지되지 않아 "모델을 한 번 올려두고 재사용"하는 이 구조와 맞지 않는다고 판단해, 팀이 이미 보유한 GCP VM에 상시 컨테이너로 올리는 방향으로 정했습니다. 데이터와 모델 캐시는 호스트 볼륨 마운트라 코드를 고쳐 재빌드해도 인덱스를 다시 만들지 않습니다.

## 9. 산출물

| 구분 | 내용 |
| --- | --- |
| 파이프라인 | `src/` — data_processing / retrieval / generation / evaluation |
| 실행·실험 스크립트 | `scripts/` — step1~27 및 진단 스크립트 40여 개 |
| 평가 결과 | `results/` — 실험별 CSV 17개 |
| 서빙 앱 | `app/main.py`, `app/static/index.html` |
| 배포 | `Dockerfile`, `docker-compose.yml`, `.dockerignore` |
| 문서 | `docs/rag-pipeline-and-eval-summary.md`, `docs/deployment-gcp-vm.md` |

브랜치 `feat/rag-pipeline-and-eval`에 8커밋으로 정리해 푸시 완료. main 병합은 PR 리뷰 대기 상태입니다.

## 10. 한계와 남은 과제

**구조적 한계 (현재 아키텍처로는 해결 불가)**

- 그림 내용이 텍스트로 추출되지 않습니다. 그림 4건 중 3건은 정답 문서를 1순위로 찾았는데도 fact coverage가 0에 수렴했습니다 — 검색 문제가 아니라 답을 만들 재료 자체가 없는 문제입니다.
- 표·그림의 위치(페이지·좌표) 정보가 청크 메타데이터에 없어 위치 단위 채점을 할 수 없습니다.
- 정답이 여러 건인 필터·집계형 질문(예: "예산 1억 이상인 사업 전부")은 top-k 검색 구조상 원리적으로 다 담기 어렵습니다. 메타데이터 필터링을 결합하는 별도 경로가 필요합니다.

**미착수·진행 중**

- chunk 단위 검색 구멍(36%) 개선 — 개선 방향 후보는 도출했으나 아직 시도하지 않았습니다.
- LLM 기반 판정(정확성·근거 충실성) 미연결 — 현재 채점 3종은 전부 결정론적이라 "답변이 실제로 맞는 내용인지"는 아직 측정하지 않았습니다.
- 평가배점은 배점표가 표 형태라 정규식으로는 위치 힌트까지만 가능 — LLM/표 파싱 전환 검토 필요.
- 시나리오 A(GCP 로컬 모델 비교) 미착수.
- 서빙 앱에 인증이 없습니다. 외부 포트 개방 대신 SSH 터널을 전제로 문서를 작성했으나, 팀 데모 방식에 따라 조정이 필요합니다.

## 11. 보고서에 쓸 만한 관점

- **측정으로 결정한 프로젝트.** 도입한 것보다 실측 후 기각한 것이 더 많습니다(리랭커 2종 기각, 가중치 튜닝 후 기존값 유지, 유료 임베딩 미채택). "왜 안 썼는가"에 전부 수치 근거가 있습니다.
- **자기 수치를 의심한 과정.** 성능이 좋게 나왔을 때 그대로 보고하지 않고 원문을 열어 확인했고, 그 결과 채점 코드 버그 5종과 doc/chunk 단위 격차를 찾아냈습니다. 09-03의 잘못된 판단을 09-06~07에 스스로 정정한 기록도 남아 있습니다.
- **평가 지표의 해상도가 결론을 바꾼다.** doc 단위로만 보면 0.97이던 검색 성능이 chunk 단위로는 0.611이었습니다. 같은 시스템을 어떤 단위로 재느냐가 "검색은 충분하다"와 "검색이 병목이다"를 갈랐습니다.
- **연구에서 제품까지 이어붙였다.** 평가 파이프라인에서 끝내지 않고 사람이 실제로 눌러볼 수 있는 화면과 배포 구성까지 만들었습니다.

# Parent-Child 충실성(faithfulness) 결정론적 프록시 측정 — 2026-09-11

> 담당: 한빈 / API 호출 **0회** / 스크립트: `scripts/step28_faithfulness_proxy.py`, `scripts/step28a_claim_divergence.py`
> 결과물: `output/faithfulness_proxy_summary.md`, `output/faithfulness_A~C_*.csv`

## 배경 — 왜 쟀나

Parent-Child 채택 근거로 그동안 제시해온 건 **context fact coverage**(공식 111건 기준
완전 커버율 36.1% → 40.7%, 평균 45.2% → 51.9%)였다. 그런데 이건 "답에 필요한 사실이
**컨텍스트 안에 들어 있느냐**"이지, "모델이 그 컨텍스트에 **근거해서 답했느냐**"가 아니다.
faithfulness는 08-27 브리핑에서 측정 대상으로 제안된 뒤 09-06까지 계속 **미연결** 상태로 남아 있었다.

측정 동기는 구체적인 가설이었다. `expand_to_parent`가 주변 2,400자를 통째로 끌어오는데,
RFP는 한 절 안에 계약보증금·지체상금·지분율처럼 **비슷한 숫자가 여러 개** 몰려 있다.
정확한 조각을 못 찾은 상태에서 맥락을 넓게 주면 **옆 조항 숫자를 끌어다 쓰는 오귀속
(misattribution)** 위험이 커질 수 있다 — 즉 Parent-Child가 커버리지는 올리면서 충실성은
깎고 있을 가능성.

## 방법 (API 0회)

기존 산출물만 재사용하는 결정론적 프록시. 답변에서 **금액·비율(%)·기간**을 정규식으로
추출해 원문 대조한다.

- 입력: `generation_eval_v3_parent_child_compare.csv`(63건, base/pc 답변 쌍),
  `chunk_level_gold_labels_v1.jsonl`(56건), `chunks.pkl`(18,239개 — child 14,572 · parent 3,664),
  `merged_docs.pkl`(98건)
- 분석 대상: gold chunk와 그 parent를 모두 확보한 **35문항**

## A. base vs pc 답변의 수치가 달라지나 (63건 전체)

| 항목 | base | parent-child |
|---|---:|---:|
| 수치 주장 총량 | 69개 | 68개 |
| 문항당 평균 | 1.10개 | 1.08개 |

서로 다른 값을 낸 문항 **2건 / 63건 (3.2%)**. 두 건 모두 "pc가 값을 하나 더 붙였다"는
유형이고, **같은 항목에 모순되는 값을 낸 경우는 0건**.

→ Parent-Child가 기존에 맞던 숫자를 틀리게 바꿔놓지는 않았다.

## B. 답변 수치 주장의 근거성 (35문항)

| | 수치 주장 | gold chunk 내 | parent 내 | 문서 어딘가 | 문서에 없음 |
|---|---:|---:|---:|---:|---:|
| base | 21개 | 17 (81.0%) | 19 (90.5%) | 19 (90.5%) | 2 |
| parent-child | 19개 | 16 (84.2%) | 17 (89.5%) | 18 (**94.7%**) | **1** |

환각 의심이 2건 → 1건으로 감소. **방향은 개선 쪽.**

확인 필요로 남긴 2건:
- `supplemental-qa-c03` 금액 `1,515,000,000원` — base만 생성, 원문 미검출
- `supplemental-qa-c09` 기간 `180일` — 양쪽 모두. 날짜에서 계산된 값일 가능성이 있어 원문 확인 필요

## C. parent 확장이 추가로 끌어오는 '경쟁 수치' (답변과 무관한 구조적 측정)

| 구분 | gold chunk | parent | 신규 유입 |
|---|---:|---:|---:|
| 컨텍스트 길이 | 2,093자 | 4,958자 (2.4배) | — |
| 금액 (서로 다른 값) | 0.46개 | 0.51개 | +0.06 |
| **비율 %** | 0.74개 | 1.31개 | **+0.57** |
| 기간 | 0.69개 | 1.03개 | +0.34 |
| **합계** | **1.89개** | **2.86개 (1.5배)** | **+0.97** |

- 경쟁 수치가 늘어난 문항: **18 / 35건 (51%)**
- gold chunk엔 수치가 0개인데 parent에는 있는 문항: **4건**

## 결론

1. **오귀속 위험은 실재하고, 크기가 측정됐다.** parent 확장으로 헷갈릴 수 있는 값이
   문항당 1.89개 → 2.86개(1.5배)로 늘고, 절반의 문항에서 증가한다. 특히 **비율(%)**이
   가장 크게 늘어난다 — RFP에서 보증금·지체상금·지분율이 한 절에 몰려 있기 때문으로 보인다.

2. **그런데 이번 표본에서는 그 위험이 실제 오답으로 발현되지 않았다.** 근거성은 오히려
   소폭 올랐고(90.5% → 94.7%), 모순된 수치를 낸 문항은 0건이다. **컨텍스트는 2.4배 늘었는데
   경쟁 수치는 1.5배만 늘었다** — parent가 더해주는 대부분이 숫자가 아닌 서술 텍스트라서
   희석 효과가 교란 효과보다 컸던 것으로 해석된다.

3. **가설을 세우고 측정했더니 가설이 틀렸던 사례.** 리랭커(실측 후 기각)·하이브리드
   가중치(기존값이 최적)와 같은 계열로, "선호가 아니라 측정으로 결정한다" 원칙이
   적용된 네 번째 케이스다. Parent-Child는 유지가 맞다.

## 한계

- 수치 주장 기반 프록시라 **서술형 주장의 충실성은 못 본다.**
- 표본이 35문항 · 수치 주장 20개 수준이라 **방향성 참고용**이며 통계적 확정은 불가.
- 실제 생성 시점의 컨텍스트가 저장돼 있지 않아 gold chunk / parent chunk로 대체 측정했다.
- 확정하려면 LLM 판정을 붙여야 한다. 팀원이 이미 만들어둔 3중 블라인드 판정 인프라에
  faithfulness 축이 가중치 0.25로 들어가 있어 재사용 가능 — rag-56 기준 on/off 합쳐 약 112회 호출.

## 최종 발표용 한 줄

> "충실성은 LLM 판정까지는 못 붙였고, 대신 결정론적 프록시로 재봤습니다. parent 확장이
> 컨텍스트를 2.4배 늘리는데 헷갈릴 수 있는 수치는 1.5배만 늘어서, 숫자 오귀속 위험이
> 실재하긴 하지만 실제 답변 근거성은 오히려 90.5%에서 94.7%로 올랐습니다. 위험을 가정하고
> 측정했더니 가정이 틀렸던 케이스입니다."
